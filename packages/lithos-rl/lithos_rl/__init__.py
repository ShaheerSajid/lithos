"""lithos_rl — RL training and inference for the layout pipeline.

Heaviest dependency footprint (torch + sb3 + gymnasium). Kept as a separate
package so users who only want the generator + heuristic repair can install
the rest without dragging this in.

Empty for now — package is scaffolded so the workspace builds.
"""
