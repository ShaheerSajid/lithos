[project]
name = "lithos-lvs"
version = "0.1.0"
description = "LVS (netgen/magic) wrappers."
requires-python = ">=3.11"
dependencies = [
    "lithos-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["lithos_lvs"]
